module.exports = (API, credentials) => {
  return new Promise((resolve, reject) => {
    const profile = {sizes: [], locations: []};
    console.log('creating profile');
    resolve(profile);
  });
};